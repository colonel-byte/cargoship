# Generated by "mage dev:vendor". Do not edit, and do not delete.
#
# package-lock.json here is the devDependencies of the web UI x/telemetry serves from its own
# repository. It is not a cargoship dependency -- go mod vendor copies it in because it sits in
# a vendored module's directory, and nothing it names is compiled, executed or shipped.
#
# OpenSSF Scorecard runs osv-scanner over the whole checkout and scores Vulnerabilities as ten
# minus the number of findings, so without this file every advisory ever filed against those
# packages costs the repository a point. See docs/agent/choice-osv-vendor-overrides.md.

[[PackageOverrides]]
ecosystem = "npm"
ignore = true
reason = "JavaScript tooling vendored alongside a Go module; not built, run or shipped by cargoship"
