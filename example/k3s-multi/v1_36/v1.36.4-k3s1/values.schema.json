{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "rancher-k3s-multi values",
  "description": "Generated in magefiles/templates/k3s/values.schema.json.tmpl.",
  "type": "object",
  "properties": {
    "addons": {
      "type": "object",
      "properties": {
        "disabled": {
          "type": "array",
          "description": "The bundled charts k3s is told not to install. Replaces the package's own list rather than adding to it. Only the components k3s 1.36 packages are accepted.",
          "items": {
            "type": "string",
            "enum": [
              "coredns",
              "servicelb",
              "traefik",
              "local-storage",
              "metrics-server",
              "runtimes"
            ]
          },
          "uniqueItems": true
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
